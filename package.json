{
  "name": "buck-writer-app",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm run --parallel --filter './packages/*' dev",
    "dev:bible": "pnpm run --parallel --filter '@buck/bible-mcp' --filter '@buck/bible-ui' dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @buck/web test:e2e",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "db:migrate": "pnpm --filter @buck/api db:migrate",
    "db:generate": "pnpm --filter @buck/api db:generate",
    "db:seed": "pnpm --filter @buck/api db:seed",
    "docker:build": "docker build -f vps/Dockerfile.app -t buck-writer:local .",
    "docker:up": "docker compose -f vps/compose.yml up -d --build",
    "docker:down": "docker compose -f vps/compose.yml down",
    "docker:logs": "docker compose -f vps/compose.yml logs -f",
    "deploy": "./vps/deploy.sh"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "prettier": "^3.3.3",
    "eslint": "^9.13.0",
    "@eslint/js": "^9.13.0",
    "@typescript-eslint/parser": "^8.12.2",
    "@typescript-eslint/eslint-plugin": "^8.12.2",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.14"
  }
}
