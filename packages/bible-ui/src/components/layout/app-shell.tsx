import type { ReactNode } from 'react';

// Stub temporaire — remplace en Task 10 par TopBar + SidebarLeft + RightPanel.
export function AppShell({ children }: { children: ReactNode }) {
  return <div>{children}</div>;
}
